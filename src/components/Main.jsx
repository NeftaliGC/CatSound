import React from "react";
import { Text, View } from "react-native";
import Constants from 'expo-constants';

const Main = () => {
    return (
        <View style={{ flex: 1, backgroundColor: "#001433", bottom: 0}}>
            <Text style={{ color:'white' }}>React Native</Text>
        </View>
    )
}

export default Main;